import { Button, Space } from 'antd';

const ReuseButton: React.FC = () => (
    <Space wrap>
        <Button style={{
            backgroundColor: "#087B2F",
            color: "white"
        }}>Save</Button>
    </Space>
);

export default ReuseButton;